import { BBox } from '../../index';
export declare function constructFixtureGetMapRequest({ bbox, width, height, format, evalscript, data, }: {
    bbox?: BBox;
    width?: number;
    height?: number;
    format?: import("../const").MimeType | "JPEG_OR_PNG";
    evalscript?: string;
    data?: any[];
}): Record<any, any>;
